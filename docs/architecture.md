# Architecture
XDV Domain Abstraction Layer is structured in two layers:
1. Stable interface layer (src/dal_interface.ds)
2. Implementation layer (src/dal.ds)
Tests from the source sector are imported into src and can be mirrored into tests as independent suites over time.
